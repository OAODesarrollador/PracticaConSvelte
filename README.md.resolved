# 📝 PracticaConSvelte - Todo List App

![Svelte](https://img.shields.io/badge/svelte-%23f1413d.svg?style=for-the-badge&logo=svelte&logoColor=white)
![Bootstrap](https://img.shields.io/badge/bootstrap-%238511FA.svg?style=for-the-badge&logo=bootstrap&logoColor=white)
![LocalStorage](https://img.shields.io/badge/-LocalStorage-lightgrey?style=for-the-badge)

Aplicación interactiva de gestión de tareas diseñada para explorar la reactividad y simplicidad de **Svelte**. Este proyecto implementa un flujo completo de creación, validación y persistencia de datos en el navegador, demostrando cómo crear interfaces de usuario ágiles y funcionales con menos código boilerplate.

🔗 **Demo Live**: [Insertar URL si existe]

## ✨ Características

-   **Reactividad Instantánea**: Actualización del DOM en tiempo real gracias al compilador de Svelte.
-   **Persistencia de Datos**: Uso de `localStorage` para mantener tus tareas guardadas entre sesiones.
-   **Validación de Entrada**: Prevención de tareas vacías con feedback visual inmediato.
-   **Notificaciones Toast**: Alertas dinámicas (Bootstrap) para confirmar acciones (crear, completar, eliminar).
-   **Interfaz Responsiva**: Diseño adaptativo utilizando el sistema de grillas y componentes de **Bootstrap 5**.

## 🛠️ Stack Tecnológico

-   **Frontend**: [Svelte 3](https://svelte.dev/)
-   **UI Framework**: [Bootstrap 5](https://getbootstrap.com/) & Bootstrap Icons
-   **Build Tool**: [Rollup](https://rollupjs.org/)
-   **Lenguaje**: JavaScript (ES6+)
-   **Gestión de Estado**: Stores y Reactividad nativa de Svelte.

## 🚀 Instalación y Uso

Sigue estos pasos para ejecutar el proyecto localmente:

1.  **Clonar el repositorio**
    ```bash
    git clone https://github.com/OAODesarrollador/PracticaConSvelte.git
    cd PracticaConSvelte
    ```

2.  **Instalar dependencias**
    ```bash
    npm install
    ```

3.  **Iniciar modo desarrollo**
    ```bash
    npm run dev
    ```

    Abre tu navegador en `http://localhost:8080` (o el puerto que indique la terminal).

4.  **Construir para producción**
    ```bash
    npm run build
    ```

## 📂 Estructura del Proyecto

```
src/
├── App.svelte      # Componente principal (Lógica + UI + Estilos)
├── main.js         # Punto de entrada de la aplicación
public/
├── index.html      # HTML base
└── global.css      # Estilos globales
```

## 📸 Capturas

| Vista Principal | Tarea Completada |
|:---:|:---:|
| ![Main View](https://placehold.co/600x400/1a1a1a/eaeaea?text=Todo+List) | ![Completed Task](https://placehold.co/600x400/1a1a1a/eaeaea?text=Toast+Notification) |

---

Desarrollado por **[Oscar Ortiz](https://github.com/OAODesarrollador)** para practicar y perfeccionar habilidades en Svelte.
